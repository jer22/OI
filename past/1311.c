#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main ( void ) {
	int n, k, p;
	
	scanf("%d %d %d", &n, &k, &p);
	int i;
	for (i = 0; i < n; i++) {
		
	}

	system("pause");
	return 0;
}
