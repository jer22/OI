#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>

int a[255], b[255];
int main ( void ) {
	long k;
	FILE * in = fopen("sretan.in", "r");
	fscanf(in, "%ld", &k);



	system("pause");
	return 0;
}
